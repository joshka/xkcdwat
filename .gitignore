# Local Rust, Wrangler, and npm outputs are reproducible from the checked-in manifests.
/target
/build
/node_modules
/.wrangler
# Local development credentials must not enter the repository.
.dev.vars*
.env*
